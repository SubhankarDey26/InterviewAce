# mlservice/.env
# Copy this to .env and adjust values

# Server
ML_HOST=0.0.0.0
ML_PORT=8000
ML_RELOAD=true          # Set to false in production
ML_WORKERS=1            # Increase to match CPU cores in production
LOG_LEVEL=info

# Model
MODEL_DIR=saved_models/deberta   # Path to fine-tuned weights